// home.service.ts

import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class HomeService {

  constructor() { }

  // Suponha que você tenha algum mecanismo para recuperar o nome do usuário, como um serviço de autenticação
  //getNomeUsuario(): string {
    // Exemplo: recuperando o nome do usuário armazenado no localStorage
    //return localStorage.getItem('nome');
 // }
}
